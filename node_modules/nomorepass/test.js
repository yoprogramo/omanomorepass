const nmp = require('./lib/nomorepass');
nmp.init({'apikey':'asdassdasdas'});
nmp.setExpiry(Math.trunc(Date.now()/1000 + 30));
nmp.getQrText('hola', (data) => {
     console.log(data);
     nmp.start((data)=>{console.log(data);});
    });
nmp.getQrSend ('WEBDEVICE','usuario','pass',{type:'pwd'}, 
    function (text){
        if (text==false){
            coneole.log("Error calling nmp");
        } else {
            // Show the qr with this text
            console.log(text);
            // wait to be scanned and received
            // by the app (optional)
            nmp.send (function(data){
                //console.log(data);
            })
        }
    }
);